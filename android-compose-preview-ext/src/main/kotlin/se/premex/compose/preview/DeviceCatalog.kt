package se.premex.compose.preview

/**
 * Device catalog root object. Google built-in device constants removed.
 * Third-party manufacturer extension properties (e.g. DeviceCatalog.Zebra) are generated separately.
 */
object DeviceCatalog {
    const val DEFAULT: String = ""
}

